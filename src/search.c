#include "../include/search.h"
#include "../include/cube.h"
#include "../sys.h"

u8 is_visted(cube *C){
    //TODO:use hash to find same cube
}

way_node *create_node(cube *C){
    way_node* new_node;
    new_node=malloc(sizeof(way_node))
    new_node->C=C;
}

int search_phase1(way_node *head){
    
}
